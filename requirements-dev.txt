# Developer / CI quality tools only (not part of product install profiles).
# Pin exact versions so PR quality gates are reproducible.
# Install: python -m pip install -r requirements-dev.txt
#
# These packages are intentionally outside requirements-lock/ — they are not
# shipped to end users and must not drift product hash locks.

ruff==0.12.5
mypy==1.17.0
pip-audit==2.9.0
